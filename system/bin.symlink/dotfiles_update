#!/usr/bin/env bash

set -e

# update dotfiles
cd "$HOME/.dotfiles"
git pull --recursive
./script/bootstrap

# update bash
$HOME/.bash-it/install.sh

# update vim bundles
$EDITOR +BundleInstall +qall

